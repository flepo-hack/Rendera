workflows:
  android-build:
    name: Android Build
    max_build_duration: 30
    environment:
      vars:
        GEMINI_API_KEY: "dummy_key_here"
    scripts:
      - name: Unzip source code
        script: |
          echo "Extracting rendera.zip..."
          unzip -o rendera.zip -d .
          echo "Project structure after extraction:"
          ls -la
      - name: Install Gradle 9.3.1
        script: |
          echo "Installing Gradle 9.3.1..."
          GRADLE_VERSION=9.3.1
          wget -q https://services.gradle.org/distributions/gradle-${GRADLE_VERSION}-bin.zip
          unzip -q gradle-${GRADLE_VERSION}-bin.zip -d /usr/local/
          export PATH=/usr/local/gradle-${GRADLE_VERSION}/bin:$PATH
          gradle --version
      - name: Build APK with correct Gradle
        script: |
          export PATH=/usr/local/gradle-9.3.1/bin:$PATH
          echo "Using Gradle: $(which gradle)"
          PROJECT_DIR=$(find . -name settings.gradle.kts -print -quit | xargs dirname)
          if [ -z "$PROJECT_DIR" ]; then
            echo "ERROR: settings.gradle.kts not found!"
            exit 1
          fi
          echo "Project directory: $PROJECT_DIR"
          cd "$PROJECT_DIR"
          # Varmistetaan, että käytetään oikeaa Gradlea, ei wrapperia
          gradle assembleDebug --no-daemon
    artifacts:
      - app/build/outputs/apk/debug/*.apk
